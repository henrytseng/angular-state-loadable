'use strict';

describe('$loadable', function() {
  var _loadable;

  beforeEach(module('angular-state-loadable'));

  beforeEach(inject(function($loadable) {
    _loadable = $loadable;
  }));

  describe('#create', function() {
    it('Should create a loadable', function() {
      
    });
  });

});
